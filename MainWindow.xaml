<Window x:Class="MM_IdealGas.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MM_IdealGas"
        mc:Ignorable="d"
        Title="2D Ideal gas" Height="400" Width="600">
    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased" Source="{Binding Frame}"/>
        <StackPanel Grid.Column="1">
            <Label Content="Количество частиц"/>
            <TextBox Text="{Binding Particles, UpdateSourceTrigger=LostFocus}"/>
            <Label Content="Коэф. для расстояния между част."/>
            <TextBox Text="{Binding Margin, UpdateSourceTrigger=LostFocus}"/>
            <Label Content="Максимальная Uo"/>
            <TextBox Text="{Binding MaxU0, UpdateSourceTrigger=LostFocus}"/>
            <Button Content="Сгенерировать" Command="{Binding Generate}" Margin="0,10,0,0"></Button>
        </StackPanel>
    </Grid>
</Window>
